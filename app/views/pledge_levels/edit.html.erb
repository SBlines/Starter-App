<% content_for :title do %>
Edit Pledge Level for <%= @campaign.name %>
<% end %>
<p>Edit Pledge Level</p>


<%= simple_form_for [@campaign, @pledge_level], :html => {:class => 'form-horizontal' } do |f| %>
	<%= f.input :description %>
	<%= f.input :amount %>
	<%= f.button :submit %>
	<% end %>
	